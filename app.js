  var platform = new Platform(document.body);

  // platform.register(UsersTable);
  // platform.register(Broadcast);
  platform.register(Calculator);
  platform.register(Well);
  platform.register(Rain);
  platform.register(WaterBox);
  platform.register(Tap);
  // platform.registerCoreApplication(Inspector);

  // platform.register(NewApplication)
